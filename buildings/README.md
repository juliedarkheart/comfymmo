# Buildings

Building definitions, placement validation, footprints, and structure behavior
live here.

Current prototype:

- `placeable_object_data.gd` describes local placeable definitions
- `placeable_crate.gd` drives the first placeable placeholder object
