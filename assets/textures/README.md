# Textures

Texture bundles cover terrain, building surfaces, and material studies.

- `generated/` holds raw generation bundles
- `tiles/` holds curated tileable textures
- `materials/` holds reusable surface families
