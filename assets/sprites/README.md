# Sprites

Sprite bundles cover avatars, creatures, props, and buildings.

- `generated/` holds raw output bundles
- `sheets/` holds sprite sheet assemblies
- `approved/` holds import-ready or nearly import-ready bundles
