# Concepts

Concept bundles set direction before production art is promoted.

- `generated/` holds raw local exploration bundles
- `approved/` holds chosen directions that downstream assets may reference
