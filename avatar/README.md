# Avatar

Player avatar movement, appearance, animation, and replicated avatar state live
here.

Current prototype:

- `avatar_controller.gd` handles local isometric movement through a multiplayer-ready input boundary.
- `avatar_camera.gd` follows the player and applies homestead camera limits.
