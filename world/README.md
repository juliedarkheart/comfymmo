# World

World code owns maps, regions, isometric coordinates, and simulation boundaries.

Current placeholders:

- `world_system.gd` provides basic grid/world coordinate conversion.

Current prototype:

- `homestead_map.gd` builds the small isometric homestead and collision obstacles.
- `homestead_controller.gd` coordinates player spawning for the homestead scene.
